# Playwright MCP Environment Variables Example
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to version control

# MCP Server Configuration
# MCP_PORT=8931
# MCP_HOST=localhost

# Browser Configuration
# BROWSER_HEADLESS=true
# BROWSER_TYPE=chromium

# Network Configuration
# PROXY_SERVER=http://proxy.example.com:3128
# PROXY_BYPASS=.local,localhost

# Security Settings
# ALLOWED_HOSTS=example.com,trusted-site.com
# BLOCK_SERVICE_WORKERS=true

# Storage
# USER_DATA_DIR=/path/to/user/data
# OUTPUT_DIR=/path/to/output

# Secrets (if using --secrets parameter)
# Path to secrets file in dotenv format
# SECRETS_FILE=/path/to/secrets.env

# Example secrets that might be in your secrets file:
# API_KEY=your_api_key_here
# AUTH_TOKEN=your_auth_token_here
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname
