# CDP endpoint for connecting to an existing Chrome browser
CDP_ENDPOINT=http://host.docker.internal:9222

# Comma-separated list of allowed domains (optional)
# Navigation and actions on other domains will be blocked
ALLOWED_DOMAINS=chat.openai.com,gemini.google.com

# Tailscale auth key (required for tailscale profile)
TAILSCALE_AUTHKEY=tskey-auth-...
